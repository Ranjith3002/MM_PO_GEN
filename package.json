{
  "name": "AI_MATERIAL_MANAGEMENT",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@cap-js/hana": "^1.2.0",
    "@sap/cds": "^8.7.1",
    "@sap/xssec": "^4",
    "axios": "^1.11.0",
    "express": "^4"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^8.7.1",
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "chai": "^4.3.10",
    "mocha": "^10.2.0"
  },
  "overrides": {
    "@sap/hdi-deploy": "5.4.2"
  },
  "scripts": {
    "start": "cds-serve",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:integration": "mocha test/integration/**/*.test.js",
    "lint": "eslint srv/ app/",
    "build": "cds build",
    "deploy": "cds deploy"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "url": "db.sqlite"
        }
      },
      "[production]": {
        "auth": "xsuaa"
      }
    }
  }
}
